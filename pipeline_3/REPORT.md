# Pipeline 3 report

**Status: FRAMEWORK READY — NO TUNING RUN EXECUTED**

The configured search contains six candidates and uses official
`vllm bench sweep serve`. No model server, sweep, benchmark, or auto-tuning command
has been run by this project setup.
